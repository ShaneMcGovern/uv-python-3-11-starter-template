[project]
name = "uv-python-3-11"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "~=3.11.0"
dependencies = []

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
    "prettier>=0.0.7",
    "pytest>=9.0.3",
    "pytest-cov>=7.0.0",
]
